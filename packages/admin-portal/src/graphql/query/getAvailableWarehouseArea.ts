import { gql } from "@apollo/client";

const GET_TOTAL_AVAILABLE_AREA_SUM = gql`
  query GET_TOTAL_AVAILABLE_AREA_SUM {
    getTotalabviableareaSum
  }
`;

export default GET_TOTAL_AVAILABLE_AREA_SUM;
